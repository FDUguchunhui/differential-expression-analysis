1. claims
files in this folder is designed as interface for the analysis of DESeq2 result, ESeqDataSet(dds), to be specific,
which means all the names are resuable. If you want to use those code as scripts to analyze your result, please clean you environment and double check whether you use the right data and object, then run the analysis scripts. However, rewritting your own code is what recommended.

2. contents
1) get_norm_data
2) subset
3) set_operation
